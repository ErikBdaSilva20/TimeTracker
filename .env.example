# ─── Frontend (browser) ───────────────────────────────────────────────
# Base do MasIA gateway. Vazio = modo preview (fixtures em memória).
# Para rodar local com o gateway do docker-compose, use:
#   VITE_GATEWAY_URL=http://localhost:8787
VITE_GATEWAY_URL=

# ─── Gateway local (local-gateway/) — usado só pelo docker-compose ─────
# DATABASE_URL=postgres://masia:masia_dev@db:5432/tenant_local
# PORT=8787
